function NavPath(): JSX.Element {
  return (
    <div className="w-full h-full flex items-center justify-center">
      <p>{"El mundo > América del Sur > Chile Región Metropolitana de Santiago > Santiago de Chile"}</p>
    </div>
  );
}

export default NavPath;

/*Fix full width border separator for NavPath */
